import { something } from './utils';

export const dummyDemo = () => {
  return 'Why are you ghey';
};

export const plus = (x: number, y: number): number => {
  return x + y;
};

export const minus = (x: number, y: number): number => {
  return x - y;
};

export const multiply = (x: number, y: number): number => {
  return x * y;
};

export const divide = (x: number, y: number): number => {
  return x / y;
};

export const logSomething = () => {
  console.log(something);
};
